begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "big_network"
    gem.summary = "Social network relationship engine."
    gem.email = "tony+bignetwork@tonystubblebine.com"
    gem.authors = ["Tony Stubblebine"]
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
  end
rescue
  puts "Jeweler or dependency not available."
end
